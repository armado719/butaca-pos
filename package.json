{
  "name": "butaca-pos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server"
  },
  "dependencies": {
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "vite-plugin-pwa": "^1.2.0"
  }
}
